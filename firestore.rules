rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For the scope of this prototype, allow read/write if the user is authenticated.
      // In a real app, this should be scoped tightly.
      allow read, write: if request.auth != null;
    }
  }
}
